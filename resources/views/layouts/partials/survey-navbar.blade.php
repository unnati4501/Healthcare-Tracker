<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <span class="user-side-logo">
            <img alt="{{ config('app.name', env('APP_NAME')) }}" class="header-logo" src="{{ $branding->company_logo }}"/>
        </span>
    </div>
</nav>